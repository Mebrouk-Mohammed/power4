<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Classement — Puissance 4</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root{
      --bg:#0f1115;
      --panel:#14161a;
      --panel2:#1b1d22;
      --text:#eaeaea;
      --accent:#ffa94d;
    }
    body{
      font-family:system-ui, Arial;
      background:var(--bg);
      color:var(--text);
      margin:0;
    }
    .wrap{
      max-width:1000px;
      margin:40px auto;
      padding:20px;
    }
    header{
      background:var(--panel);
      padding:16px 20px;
      border:1px solid #23283a;
      border-radius:12px;
    }
    table{
      width:100%;
      border-collapse:collapse;
      margin-top:16px;
    }
    th,td{
      padding:12px;
      text-align:left;
    }
    th{
      background:var(--panel2);
      border-bottom:2px solid var(--accent);
      font-weight:600;
      font-size:14px;
    }
    tr:nth-child(even){
      background:#14161a;
    }
    .avatar{
      width:40px;
      height:40px;
      border-radius:8px;
      object-fit:cover;
      vertical-align:middle;
      margin-right:10px;
    }
    a{
      color:var(--accent);
      text-decoration:none;
    }

    /* Bouton retour */
    .btn-row{
      margin-bottom:16px;
    }
    .btn-ghost{
      display:inline-block;
      padding:8px 14px;
      border-radius:999px;
      border:1px solid #2b303f;
      background:#14161a;
      color:var(--accent);
      font-size:14px;
      text-decoration:none;
    }
    .btn-ghost:hover{
      background:#1d2028;
    }

    /* Rangs */
    .rank-pill{
      display:inline-block;
      padding:4px 10px;
      border-radius:999px;
      font-size:12px;
      font-weight:600;
    }
    .rank-Bronze{
      background:#3b2a18;
      color:#ffb347;
    }
    .rank-Silver{
      background:#262a33;
      color:#c0cad8;
    }
    .rank-Gold{
      background:#3b3214;
      color:#ffd86b;
    }
    .rank-Platine{
      background:#1f323c;
      color:#8cf1ff;
    }
    .rank-Diamant{
      background:#182b3d;
      color:#a5f3ff;
    }
    .rank-Master{
      background:#351b45;
      color:#f3b4ff;
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="btn-row">
      <a class="btn-ghost" href="/legacy">⬅️ Retour au menu</a>
    </div>

    <header>
      <h1>🏆 Classement des joueurs — Puissance 4</h1>
    </header>

    <table>
      <tr>
        <th>#</th>
        <th>Joueur</th>
        <th>Rang</th>
        <th>ELO</th>
        <th>Parties</th>
        <th>Victoires</th>
        <th>Défaites</th>
        <th>Nuls</th>
      </tr>
      {{ if .Players }}
        {{ range $i, $p := .Players }}
          <tr>
            <td>{{ add $i 1 }}</td>
            <td>
              <a href="/public_profile?username={{ $p.Username }}">
                <img src="{{ $p.Avatar }}" class="avatar" alt="">{{ $p.DisplayName }}
              </a>
            </td>
            <td>
              <span class="rank-pill rank-{{ $p.Rank }}">{{ $p.Rank }}</span>
            </td>
            <td>{{ $p.Rating }}</td>
            <td>{{ $p.GamesPlayed }}</td>
            <td>{{ $p.Wins }}</td>
            <td>{{ $p.Losses }}</td>
            <td>{{ $p.Draws }}</td>
          </tr>
        {{ end }}
      {{ else }}
        <tr>
          <td colspan="8" style="text-align:center;padding:20px;">
            Aucun joueur trouvé.
          </td>
        </tr>
      {{ end }}
    </table>
  </div>
</body>
</html>
