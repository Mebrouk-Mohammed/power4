{{define "board_large"}}
<<<<<<< HEAD
<div class="board neon-board-large">
  <div class="board-frame-neon-large">
    <div class="board-inner-neon-large"
         style="--rows: {{.Rows}}; --cols: {{.Cols}};
                --cell: 50px; --gap: 5px;">
                
      <form action="/play" method="post">
        <div class="controls neon-controls-large">
          {{range $c := rangeN .Cols}}
            <button class="colbtn neon-btn-large" name="col" value="{{$c}}" {{if ne $.Winner 0}}disabled{{end}}>▼</button>
          {{end}}
        </div>

        <div class="game-grid-neon-large">
          {{range $i, $row := .Board}}
            {{range $j, $cell := $row}}
              <button name="col" value="{{$j}}" type="submit" class="hole-neon-large"
                      {{if ne $.Winner 0}}disabled{{end}}>
                {{if eq $cell 1}}
                  <div class="token-p1-neon-large">{{template "token_p1" $}}</div>
                {{else if eq $cell 2}}
                  <div class="token-p2-neon-large">{{template "token_p2" $}}</div>
                {{end}}
              </button>
            {{end}}
          {{end}}
        </div>
      </form>
    </div>
    <div class="board-base-neon-large"></div>
    <div class="board-feet-neon-large">
      <div class="board-foot-neon-large"></div>
      <div class="board-foot-neon-large"></div>
    </div>
  </div>
=======
<style>
  .grid{display:grid;grid-template-columns:repeat({{.Cols}},62px);gap:12px;justify-content:center;margin:8px 0 14px}
  .dropbtn{width:62px;height:38px;border-radius:10px;border:1px solid #2a355a;background:#1a2547;color:#a8dadc;cursor:pointer;transition:.15s}
  .dropbtn:hover{background:#213055;color:#e9fbff;transform:translateY(-1px)}
  .cells{display:grid;grid-template-columns:repeat({{.Cols}},62px);gap:12px;justify-content:center}
  .cell{width:62px;height:62px;border-radius:50%;background:#0d1a33;display:grid;place-items:center;box-shadow:inset 0 0 0 2px #2a355a}
  .empty{opacity:.25}
</style>

<div class="grid">
  {{range $c := rangeN .Cols}}
    <form method="post" action="/play" style="margin:0">
      <input type="hidden" name="col" value="{{$c}}">
      <button class="dropbtn" type="submit">↓</button>
    </form>
  {{end}}
</div>

<div class="cells">
  {{range $r := rangeN .Rows}}
    {{range $c := rangeN .Cols}}
      {{ $v := index (index $.Board $r) $c }}
      <div class="cell">
        {{if eq $v 1}}
          {{template "token_p1" $}}
        {{else if eq $v 2}}
          {{template "token_p2" $}}
        {{else}}
          <span class="empty">●</span>
        {{end}}
      </div>
    {{end}}
  {{end}}
>>>>>>> bda513fd2bb1669761e3605ed8a5539f7056ce17
</div>

<style>
/* Plateau néon bleu - Version Large (Pro) */
.neon-board-large {
  background: radial-gradient(ellipse at center, #1a2854 0%, #0f1b3c 50%, #0a1429 100%);
  padding: 60px;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 800px;
}

.board-frame-neon-large {
  background: rgba(8, 18, 45, 0.95);
  border: 4px solid #00b4ff;
  border-radius: 20px;
  padding: 35px;
  box-shadow: 
    0 0 35px rgba(0, 180, 255, 0.6),
    0 0 70px rgba(0, 180, 255, 0.3),
    0 10px 40px rgba(0, 0, 0, 0.5),
    inset 0 0 35px rgba(0, 80, 160, 0.25);
  position: relative;
  transform: perspective(1200px) rotateX(4deg);
}

.board-frame-neon-large::before {
  content: '';
  position: absolute;
  top: -5px;
  left: -5px;
  right: -5px;
  bottom: -5px;
  background: linear-gradient(45deg, #00b4ff, #0099dd, #00b4ff, #00ccff, #00b4ff);
  border-radius: 25px;
  z-index: -1;
  opacity: 0.8;
  filter: blur(4px);
}

.board-frame-neon-large::after {
  content: '';
  position: absolute;
  top: -8px;
  left: -8px;
  right: -8px;
  bottom: -8px;
  background: linear-gradient(90deg, transparent, #00b4ff, transparent);
  border-radius: 28px;
  z-index: -2;
  opacity: 0.4;
  filter: blur(6px);
}

.board-inner-neon-large {
  position: relative;
}

.neon-controls-large {
  display: grid;
  grid-template-columns: repeat(var(--cols), 1fr);
  gap: 12px;
  margin-bottom: 30px;
}

.neon-btn-large {
  padding: 12px;
  background: rgba(0, 180, 255, 0.25);
  border: 3px solid #00b4ff;
  border-radius: 12px;
  color: #00ccff;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 
    0 0 15px rgba(0, 180, 255, 0.4),
    inset 0 2px 4px rgba(255, 255, 255, 0.1);
  font-size: 16px;
  text-shadow: 0 0 8px rgba(0, 180, 255, 0.8);
}

.neon-btn-large:hover:not(:disabled) {
  background: rgba(0, 180, 255, 0.5);
  box-shadow: 
    0 0 25px rgba(0, 180, 255, 0.6),
    inset 0 0 10px rgba(0, 180, 255, 0.3);
  transform: translateY(-4px) scale(1.02);
  border-color: #00ccff;
  text-shadow: 0 0 12px rgba(0, 180, 255, 1);
}

.neon-btn-large:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}

.game-grid-neon-large {
  display: grid;
  grid-template-columns: repeat(var(--cols), var(--cell));
  grid-template-rows: repeat(var(--rows), var(--cell));
  gap: var(--gap);
  background: rgba(12, 28, 65, 0.95);
  padding: 30px;
  border-radius: 18px;
  border: 3px solid rgba(0, 180, 255, 0.8);
  box-shadow: 
    inset 0 0 30px rgba(0, 50, 120, 0.5),
    inset 0 0 60px rgba(0, 0, 0, 0.3);
}

.hole-neon-large {
  width: var(--cell);
  height: var(--cell);
  border-radius: 50%;
  background: radial-gradient(circle at 35% 35%, 
    rgba(0, 120, 220, 0.2) 0%, 
    rgba(8, 18, 45, 0.95) 50%, 
    rgba(0, 40, 100, 0.9) 100%);
  border: 3px solid #00b4ff;
  box-shadow: 
    0 0 18px rgba(0, 180, 255, 0.4),
    inset 0 0 18px rgba(0, 60, 140, 0.6),
    inset 0 5px 10px rgba(0, 180, 255, 0.25),
    inset 0 -4px 8px rgba(0, 0, 0, 0.7);
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
  padding: 0;
  position: relative;
}

.hole-neon-large::before {
  content: '';
  position: absolute;
  width: calc(var(--cell) - 12px);
  height: calc(var(--cell) - 12px);
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, 
    rgba(0, 180, 255, 0.15) 0%, 
    transparent 60%);
  border: 1px solid rgba(0, 180, 255, 0.3);
  pointer-events: none;
}

.hole-neon-large::after {
  content: '';
  position: absolute;
  width: calc(var(--cell) - 8px);
  height: calc(var(--cell) - 8px);
  border-radius: 50%;
  box-shadow: inset 0 0 15px rgba(0, 0, 0, 0.9);
  background: radial-gradient(circle at center, 
    transparent 0%, 
    rgba(0, 0, 0, 0.5) 70%);
  pointer-events: none;
}

.hole-neon-large:hover:not(:disabled) {
  box-shadow: 
    0 0 25px rgba(0, 180, 255, 0.7),
    inset 0 0 20px rgba(0, 100, 255, 0.5);
  border-color: #00ccff;
  transform: scale(1.08);
  background: radial-gradient(circle at 35% 35%, 
    rgba(0, 150, 255, 0.3) 0%, 
    rgba(8, 18, 45, 0.95) 50%, 
    rgba(0, 60, 120, 0.9) 100%);
}

.token-p1-neon-large, .token-p2-neon-large {
  width: calc(var(--cell) - 6px);
  height: calc(var(--cell) - 6px);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 4;
}

.board-base-neon-large {
  width: 100%;
  height: 45px;
  background: linear-gradient(to bottom, 
    rgba(0, 180, 255, 0.9) 0%, 
    rgba(0, 140, 220, 1) 20%, 
    rgba(0, 120, 200, 1) 50%,
    rgba(0, 100, 180, 1) 80%,
    rgba(0, 80, 160, 1) 100%);
  border-radius: 0 0 18px 18px;
  box-shadow: 
    0 10px 25px rgba(0, 180, 255, 0.5),
    inset 0 5px 10px rgba(255, 255, 255, 0.2),
    inset 0 -5px 10px rgba(0, 0, 0, 0.4);
  border: 4px solid #00b4ff;
  border-top: none;
  margin-top: -3px;
  position: relative;
}

.board-base-neon-large::before {
  content: '';
  position: absolute;
  top: 4px;
  left: 12%;
  right: 12%;
  height: 5px;
  background: linear-gradient(to right, 
    transparent, 
    rgba(255, 255, 255, 0.5), 
    transparent);
  border-radius: 3px;
}

.board-feet-neon-large {
  display: flex;
  justify-content: space-between;
  padding: 0 55px;
  margin-top: -4px;
}

.board-foot-neon-large {
  width: 60px;
  height: 30px;
  background: linear-gradient(to bottom, 
    rgba(0, 140, 220, 1) 0%, 
    rgba(0, 120, 200, 1) 30%,
    rgba(0, 100, 180, 1) 70%,
    rgba(0, 80, 160, 1) 100%);
  border-radius: 0 0 15px 15px;
  box-shadow: 
    0 6px 18px rgba(0, 180, 255, 0.4),
    inset 0 3px 6px rgba(255, 255, 255, 0.25);
  border: 3px solid #00b4ff;
  border-top: none;
}

/* Animation premium pour large */
@keyframes neon-pulse-large {
  0%, 100% { 
    box-shadow: 
      0 0 35px rgba(0, 180, 255, 0.6),
      0 0 70px rgba(0, 180, 255, 0.3);
  }
  50% { 
    box-shadow: 
      0 0 50px rgba(0, 180, 255, 0.8),
      0 0 100px rgba(0, 180, 255, 0.5);
  }
}

.board-frame-neon-large:hover {
  animation: neon-pulse-large 1.8s ease-in-out infinite;
}

/* Effet de respiration sur les trous */
@keyframes hole-breathe {
  0%, 100% { border-color: #00b4ff; }
  50% { border-color: #00ccff; }
}

.hole-neon-large {
  animation: hole-breathe 4s ease-in-out infinite;
}
</style>
{{end}}