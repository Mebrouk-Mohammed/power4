<!DOCTYPE html>
<!-- Déclaration du type de document HTML5 -->
<html lang="fr">
<head>
  <!-- Encodage des caractères en UTF-8 -->
  <meta charset="UTF-8">
  <!-- Titre affiché dans l'onglet du navigateur -->
  <title>Jeu Puissance 4</title>
  <style>
    /* Réinitialise les marges et définit la hauteur de la page au maximum */
    html, body {
      margin: 0;            /* Supprime les marges par défaut */
      padding: 0;           /* Supprime les espacements internes par défaut */
      height: 100%;         /* Prend toute la hauteur de la fenêtre */
      background-color: #000; /* Fond noir pour l'arrière-plan général */
    }

    /* Conteneur principal qui affiche le plateau en image de fond */
    .board {
      background-image: url("/static/difficulte_facile.png"); /* Image de fond du plateau (mode facile) */
      background-size: cover;        /* L'image couvre toute la zone, quitte à être recadrée */
      background-position: center;   /* Centre l'image dans le conteneur */
      width: 100%;                   /* Prend toute la largeur de la fenêtre */
      height: 100%;                  /* Prend toute la hauteur de la fenêtre */
      display: flex;                 /* Utilise Flexbox pour centrer le contenu */
      justify-content: center;       /* Centre horizontalement le contenu */
      align-items: center;           /* Centre verticalement le contenu */
    }

    /* Style du texte "À toi de jouer !" */
    h1 {
      color: white;                              /* Texte blanc pour le contraste */
      font-size: 3em;                            /* Grande taille de police */
      background-color: rgba(0,0,0,0.5);         /* Fond noir semi-transparent derrière le texte */
      padding: 20px;                             /* Espace interne autour du texte */
      border-radius: 10px;                       /* Coins arrondis pour le bloc de texte */
    }
  </style>
</head>
<body>
  <!-- Conteneur qui affiche l'image de plateau en fond -->
  <div class="board">
    <!-- Message central pour le joueur -->
    <h1>À toi de jouer !</h1>
  </div>
</body>
</html>
