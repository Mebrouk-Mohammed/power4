<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Créer un compte — Power4</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    :root{--bg:#0f1115;--panel:#161a22;--text:#e8e8e8;--accent:#ffa94d;--border:#232839}
    body{background:var(--bg);color:var(--text);font-family:system-ui,Arial;margin:0}
    .wrap{max-width:800px;margin:40px auto;padding:20px}
    .card{background:var(--panel);border:1px solid var(--border);border-radius:12px;padding:24px;max-width:520px;margin:40px auto}
    h1{margin:0 0 16px 0;color:var(--accent);text-align:center}
    input{width:100%;padding:12px;border-radius:8px;border:1px solid #2b3140;background:#0f1420;color:var(--text);margin-bottom:12px}
    button{width:100%;padding:12px;border-radius:8px;border:none;background:var(--accent);color:#111;font-weight:700;cursor:pointer}
    .small{font-size:13px;color:#9aa6b2;text-align:center;margin-top:12px}
    .error{background:#3b2121;padding:8px;border-radius:8px;color:#ffb3b3;margin-bottom:12px;text-align:center}
    a.link{color:var(--accent);text-decoration:none}
  </style>
</head>
<body>
  <div class="wrap">
    <div style="text-align:left;margin-bottom:12px"><a class="link" href="/legacy">⬅️ Retour au menu</a></div>
    <div class="card">
      <h1>Créer un compte</h1>
      {{if .}}<div class="error">{{.}}</div>{{end}}
      <form method="POST">
        <input name="username" placeholder="Nom d'utilisateur" required autofocus>
        <input name="email" type="email" placeholder="Email (optionnel)">
        <input name="password" type="password" placeholder="Mot de passe" required>
        <button type="submit">S'inscrire</button>
      </form>
      <div class="small">Déjà inscrit ? <a class="link" href="/login">Se connecter</a></div>
    </div>
  </div>
</body>
</html>